#TODO 3. Update the example.properties file with the keys that you added to the application.properties file, but not the actual values (i.e. don't put your real database credentials here).

spring.datasource.url=jdbc:mysql://localhost/example_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=example_user
spring.datasource.password=example_p@$$w0rd
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect